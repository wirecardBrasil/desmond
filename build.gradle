subprojects {
    buildscript {
        ext {
            kotlinVersion = '1.2.71'
        }

        repositories {
            jcenter()
            mavenCentral()
        }

        dependencies {
            classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        }
    }
}